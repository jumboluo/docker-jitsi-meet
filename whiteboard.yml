
services:
    whiteboard:
        image: jitsi/excalidraw-backend:2025.8.2
        container_name: meet-whiteboard
        restart: ${RESTART_POLICY:-unless-stopped}
        depends_on:
            - web
        ports:
            - 3002:80
        networks:
            meet.jitsi:
              aliases:
                  - whiteboard.meet.jitsi
